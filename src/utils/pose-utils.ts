// TODO: Update pose-utils to use new React app types  
// Temporarily disabled pose-utils to avoid build errors
// Will be updated to work with new pose detection system

// Placeholder exports to avoid build errors
export const comparePoses = null;
export const detectRaisedArms = null;
export const calculateStability = null;